{
	"name": "autoplay-fork",
	"version": "1.0",
	"description": "This plugin is a fork of thomas.doerr's autostart plugin that was never update for Volumio v3. It starts the first entry in the queue or from the last played position after rebooting volumio.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Thomas Doerr",
	"license": "ISC",
	"volumio_info": {
		"prettyName": "AutoStart",
		"icon": "fa-play-circle-o",
		"plugin_type": "system_controller",
		"boot_priority": 10
	},
	"dependencies": {
		"kew": "^0.7.0",
		"v-conf": "^1.4.2"
	}
}
